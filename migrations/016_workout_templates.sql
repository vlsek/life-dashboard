-- 016_workout_templates.sql
--
-- Поддержка типовых программ тренировок (шаблонов): при применении шаблона в
-- workout_exercises создаются упражнения с рекомендованной схемой подходов
-- (например "3×8-12" или "3×30-45 сек") — чисто справочный текст, реальный
-- прогресс (вес/повторения по факту) человек вводит сам через обычные записи.

alter table workout_exercises add column if not exists suggested_scheme text;
